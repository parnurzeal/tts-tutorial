name: festival_sinhala
description: Build sinhala voice
inputParameters:
- name: INPUT_WAV
  defaultValue: gs://nemo-pipeline/smoke-test/sinhala-wav-small.tar.gz
  localCopy:
    disk: datadisk
    path: wav.tar.gz
outputParameters:
- name: OUTPUT_VOICE
  defaultValue: gs://nemo-pipeline/smoke-test/sinhala-built-voice-small.tar.gz
  localCopy:
    disk: datadisk
    path: built_voice.tar.gz
resources:
  minimumCpuCores: 16
  minimumRamGb: 60
  disks:
  - name: datadisk
    type: PERSISTENT_HDD
    sizeGb: 100
    mountPoint: /mnt/data
docker:
  imageName: parnurzeal/language-resources:ver1
  cmd: "/language-resources-entrypoint.sh build_voice ${INPUT_WAV} si ${OUTPUT_VOICE}"
